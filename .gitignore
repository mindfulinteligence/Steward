/_build
/cover
/deps
/doc
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Environment files (local secrets, never committed)
.env*
!.env.example
!.env.remote
!.env.multitenant

# ACS generated files (local, not committed)
steward.env
steward.docker-compose.yml
AGENTS_STEWARD.md
var/
acs_data/

# Metaanalysis artifacts (auto-generated)
metaanalysis/

# Auto-generated agent memory files (never committed)
priv/acs_memory/

# Test artifacts
/tmp/
priv/acs_time_offset.txt
priv/acs_developer_name.txt
priv/acs_next_agent.txt

# Cursor project MCP (contains ACS + Infisical machine identity — never commit)
.cursor/mcp.json

# Infisical host agent + generated inject file (prod server / local experiments)
.infisical.env
.env.infisical

# opencode Infisical machine identity (same creds as .cursor/mcp.json — never commit)
.opencode/infisical-client-id
.opencode/infisical-client-secret
.opencode/steward-token

# Server credentials
serverpass.md

# Steward agent secrets
steward_agent/.env

# TLS certificates (not committed)
certs/
